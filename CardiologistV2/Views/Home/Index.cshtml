@{
    ViewBag.Title = "Home Page";
}
@section featured {
    <section class="featured">
      <div class="content-wrapper">
            <hgroup class="title">
                <h1>@ViewBag.Title.</h1>
                <h2>@ViewBag.Message</h2>
            </hgroup>
            <p>
           </p>
        </div>
    </section>
}

 @*@if (User.IsInRole("Patient"))
                            {
      <nav>
                             <ul id="menu">
                                <li>
                                       @Html.ActionLink("Add a Record", "Create","Record")
                                </li>    
                                <li>
                                       @Html.ActionLink("Ask a Doctor !", "Create","Consultation")
                                </li>
                                <li>
                                       @Html.ActionLink("Add Your Medical History", "Create","History")
                                </li>
                                 </ul>
          </nav>
                                    
                            }
                            @if (User.IsInRole("Doctor"))
                            {
                                <nav>
                                    <ul id="menu">
                                         <li>
                                       @Html.ActionLink("Check Who Asked For Your Advice", "Index","Consultation")
                                        </li> 
                                       </ul>
                                </nav>
                            }
     *@     



<h3>We suggest the following:</h3>
<ol class="round">
    <li class="one">
        <h5>Classifying ECG</h5>
        
        
    </li>

    <li class="two">
        <h5>Early Warning In Case of Unhealthy Heart</h5>
        
    </li>

    <li class="three">
        <h5>Ask Our Doctors</h5>
    </li>
</ol>
